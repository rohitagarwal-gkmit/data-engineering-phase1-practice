class Test_class:
    """A test class for module_1."""

    def add(a: int, b: int) -> int:
        """Adds two integers and returns an integer.
        Args:
            a (int): First integer.
            b (int): Second integer.
        Returns:
            int: The sum of the two integers.
        """
        return a + b
